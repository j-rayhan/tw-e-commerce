import * as React from 'react';
import {View, Text} from 'react-native';

const Setting = () => {
   return (
       <View className='flex-1 justify-center items-center'>
           <Text className='text-4xl'>
             Edit Setting.tsx to change this screen.
           </Text>
       </View>
   );
};

export default Setting;